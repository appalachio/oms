<h1 class="display-4 fw-semibold mt-5"><%= title "Organizations" %></h1>
<h4 class="display-6 fw-medium mt-2"><%= page_entries_info @organizations %></h2>
<% set_meta_tags description: "An index of all the organizations." %>

<div class="border-bottom border-1 pt-2 pb-2 mb-2"></div>

<div class="btn-group mb-3" role="group" aria-label="Actions you can perform on organizations">
  <%= link_to :back, class: "btn btn-secondary" do %>
    <i class="bi bi-caret-left-square"> Back</i>
  <% end %>
  <% if user_signed_in? %>
    <%= link_to new_page_path, class: "btn btn-primary" do %>
      <i class="bi bi-plus-circle"> New Organization</i>
    <% end %>
  <% end %>
</div>

<div class="d-flex justify-content-center">
  <%= paginate @organizations %>
</div>

<div id="pages" class="list-group">
  <% @organizations.each do |organization| %>
    <%= render organization %>
  <% end %>
</div>

<div class="d-flex justify-content-center mt-3">
  <%= paginate @organizations %>
</div>
<% set_meta_tags(prev: path_to_prev_page(@organizations)) %>
<% set_meta_tags(next: path_to_next_page(@organizations)) %>
